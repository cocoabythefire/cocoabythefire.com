!!!
%html
  %head
    %title= yield(:window_title) || "Cocoa by the Fire"
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
    = analytics_init if Rails.env.production?
  %body{ id: controller_name, class: action_name }
    = yield


